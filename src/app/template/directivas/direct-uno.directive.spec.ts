import { DirectUnoDirective } from './direct-uno.directive';

describe('DirectUnoDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectUnoDirective();
    expect(directive).toBeTruthy();
  });
});
